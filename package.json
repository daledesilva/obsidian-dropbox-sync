{
  "name": "obsidian-dropbox-sync",
  "version": "1.0.2",
  "description": "Dropbox sync plugin for Obsidian",
  "main": "main.js",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun --watch build.ts -- --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "qa:generate": "node qa-test-vault/generate.mjs",
    "qa:reset": "node qa-test-vault/generate.mjs",
    "qa:deploy": "bun run build && mkdir -p \"${SYNC_TESTER_VAULT:-$HOME/Documents/sync-tester}/.obsidian/plugins/dropbox-sync\" && cp dist/* \"${SYNC_TESTER_VAULT:-$HOME/Documents/sync-tester}/.obsidian/plugins/dropbox-sync/\""
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.0.0",
    "@typescript-eslint/parser": "^8.57.0",
    "eslint": "^9.39.4",
    "eslint-plugin-obsidianmd": "^0.1.9",
    "obsidian": "^1.7.2",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.57.0"
  },
  "dependencies": {
    "localforage": "^1.10.0"
  }
}
